# 数据导入和Event Details功能总结

## 新增功能

### 1. 自定义JSON格式导入支持
支持导入你的JSON格式数据，包含以下字段：
- `date`: 日期 (YYYY-MM-DD格式)
- `time`: 时间段 (例如 "1pm-2pm")
- `tag`: 事件标签/详情
- `type`: 分类 (映射到category)
- `project`: 项目名称数组

**导入示例：**
```json
[
  {
    "date": "2025-09-02",
    "time": "1pm-2pm",
    "tag": "卡尔蔡司hr screening",
    "type": "学习",
    "project": ["Land a Job Offer"]
  },
  {
    "date": "2025-09-03",
    "time": "2pm-3pm",
    "tag": "面试准备",
    "type": "学习",
    "project": ["Land a Job Offer"]
  }
]
```

导入时会自动：
- 将所有唯一的project名称创建为Project
- 将所有唯一的type创建为Category，并自动分配颜色
- 根据date和time创建Event，duration自动计算
- tag保存为event的details字段

### 2. Event详情/Tag字段
在add/edit event的modal中新增了两个字段：

**Details / Tag**
- 用于保存event的详细信息或标签
- 支持多行输入
- 会被保存到event的details字段

**Category**
- 显示所有已保存的category
- 可以为event分配category
- Category会用不同颜色显示
- 点击category chip可以快速选择

### 3. 自动保存到本地存储
- 所有数据更改会自动保存到设备本地存储
- 通过AsyncStorage实现
- 应用重启时自动加载已保存数据

## 数据流向

```
导入 JSON (自定义格式)
  ↓
transformCustomFormat() 转换
  ↓
自动创建Projects和Categories
  ↓
创建Events（带details和category）
  ↓
自动保存到本地存储
  ↓
CalendarView显示所有event
```

## EventItem 数据结构更新

```typescript
type EventItem = {
  id: number;
  title: string;           // Event标题（通常是project名称）
  start: number;           // 开始时间（分钟）
  duration: number;        // 持续时间（分钟）
  hexColor: string;        // 颜色值
  details?: string;        // NEW: 详细信息/tag
  category?: string;       // NEW: 分类
};
```

## 文件变更

### 新增文件
- `utils/storage.ts` - 数据存储和导入转换逻辑
- `hooks/useAppData.ts` - 自动保存hook

### 修改文件
- `app/index.tsx`:
  - EventItem类型添加details和category字段
  - DraftEvent类型添加details和category字段
  - CalendarView添加categories prop
  - Event modal添加Details和Category选择UI
  - DataView支持自定义格式导入

### 新增依赖
- `@react-native-async-storage/async-storage` - 本地数据持久化

## 使用流程

### 导入自定义格式数据

1. 打开应用，切换到 **Data** 标签页
2. 将你的JSON数据（数组格式）粘贴到导入框中
3. 应用会自动识别格式并导入
4. 页面会显示导入成功，并列出导入的item数量

### 添加/编辑Event

1. 在Calendar中点击时间槽创建new event
2. 设置开始和结束时间
3. 选择项目 (可创建新项目)
4. 在 **Details / Tag** 字段输入event的详细信息
5. 在 **Category** 字段选择分类（可选）
6. 点击"Add Event"或"Update Event"保存

### 导出数据

1. 打开 **Data** 标签页
2. 点击 "📥 Export as JSON"
3. 获取标准格式的JSON数据
4. 可用于备份或与其他设备同步

## 技术实现细节

### 时间解析
自定义格式中的时间字符串（如"1pm-2pm"）会被自动解析为：
- 开始小时：1 (下午1点 = 13:00)
- 结束小时：2 (下午2点 = 14:00)
- 转换为24小时制计算start和duration

### 颜色分配
- Projects和Categories会自动分配预定义的颜色
- 每个unique的type/category获得一个唯一颜色
- 颜色从APP_COLORS中循环分配

### 数据存储
- 使用AsyncStorage作为本地数据库
- 存储键：`action_app_data`
- 自动在后台保存所有changes
- 应用启动时自动加载上次保存的数据

## 故障排查

### 导入失败
- 确保JSON格式正确（应为数组）
- 检查date格式为YYYY-MM-DD
- 检查time格式为"HHam/pm-HHam/pm"
- Tag和type字段可选

### 数据未显示
- 检查Calendar标签页的日期是否与导入的date匹配
- 确保项目已成功创建（在Graph标签页查看）
- 检查Event的时间范围（start < end）

### 本地存储问题
- 本地存储数据会自动保存，无需手动操作
- 若要重置所有数据，可在Data标签页导入新数据
- 建议先Export备份再进行大的修改
